<section class="experience wrap" aria-label="La experiencia Molina's">
  <div class="exp-grid">
    <!-- Video/Foto -->
    <div class="exp-photo-wrap anim floating">
      <video
        class="exp-photo"
        autoplay
        loop
        muted
        playsinline
        poster="/photos/foto-barbero.jpg"
      >
        <source src="/photos/video-experiencia.mp4" type="video/mp4" />
        <img
          src="/photos/foto-barbero.jpg"
          alt="Fachada de Molina's Barbería"
          class="exp-photo"
          loading="lazy"
        />
      </video>
      <div class="exp-photo-tag glass-panel">📍 Maestranza 1703</div>
    </div>

    <!-- Texto -->
    <div class="exp-text anim d1">
      <p class="section-eyebrow">La experiencia</p>
      <h2 class="section-title display">
        Aquí no vienes<br />a cortarte el pelo.
      </h2>
      <p class="section-body">
        Vienes a elevar tu imagen. Cortes de autor, ambiente exclusivo, servicio
        personalizado y los detalles que hacen única a la barbería más top de
        San Antonio.
      </p>

      <ul class="exp-list" role="list">
        <li class="exp-list-item">
          <span class="exp-check" aria-hidden="true">✓</span>
          Cortes italianos y cambios de look
        </li>
        <li class="exp-list-item">
          <span class="exp-check" aria-hidden="true">✓</span>
          Manicura para hombres
        </li>
        <li class="exp-list-item">
          <span class="exp-check" aria-hidden="true">✓</span>
          Ambiente y música en vivo
        </li>
      </ul>

      <a
        href="https://instagram.com/molinasbarberia"
        class="exp-ig-link btn btn-ghost"
        target="_blank"
        rel="noopener noreferrer"
      >
        Ver trabajos en Instagram →
      </a>
    </div>
  </div>
</section>

<style>
  .experience {
    padding-block: 4rem;
  }
  .exp-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 3rem;
  }
  .exp-photo-wrap {
    position: relative;
    border-radius: var(--r-xl);
    overflow: hidden;
    aspect-ratio: 4/5;
    max-height: 500px;
    box-shadow: 0 20px 50px rgba(0, 0, 0, 0.5);
  }
  .exp-photo {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: top center;
    transition: transform 1.5s ease;
  }
  .exp-photo-wrap:hover .exp-photo {
    transform: scale(1.05);
  }
  .exp-photo-tag {
    position: absolute;
    bottom: 1rem;
    left: 1rem;
    padding: 0.5rem 1rem;
    border-radius: var(--r-full);
    font-size: 0.8rem;
    font-weight: 600;
    color: var(--text);
  }
  .exp-list {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    margin-bottom: 2.5rem;
  }
  .exp-list-item {
    display: flex;
    align-items: center;
    gap: 1rem;
    font-size: 0.95rem;
    font-weight: 500;
    color: var(--text);
  }
  .exp-check {
    color: var(--gold);
    font-weight: 900;
    background: var(--gold-subtle);
    width: 24px;
    height: 24px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 50%;
  }
  .exp-ig-link {
    width: 100%;
  }
</style>